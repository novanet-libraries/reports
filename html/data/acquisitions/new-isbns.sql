SELECT DISTINCT ISBN
FROM WEBREPORT.ISBNS
NATURAL JOIN WEBREPORT.ADM_BIB_LOOKUP
NATURAL JOIN WEBREPORT.ITEM_RECORDS
WHERE OPEN_DATE >= :STARTDATE
  AND SUB_LIBRARY = :SUBLIB
  AND COLLECTION IN ( :COLLECTIONS )
